<div class="row">
  <div class="col-lg-3"></div>
  <div class="col-lg-6">
    <%= simple_form_for(@contact_entry,
                        :url => configuration_contact_path(@contact),
                        :html => { :class => 'form-horizontal' },
                        :method => :put) do |f| %>
        <div class="page-header">
          <h3>Edit Contact<small></small>
        <span class="pull-right">
          <input type="submit" class="btn btn-primary" value="Save" />
          <%= link_to 'Cancel', params[:return_url] || configuration_contacts_path(), class: 'btn btn-default' %>
        </span>
          </h3>
        </div>
        <%= render :partial => 'form',
                   :locals => { :form => f,
                                :contact_entry => @contact_entry }
        %>
    <% end %>
  </div>
  <div class="col-lg-3"></div>
</div>
